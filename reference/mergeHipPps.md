# mergeHipPps

mergeHipPps

## Usage

``` r
mergeHipPps(HIP, PPSEpisode, PPSMinMax, cdm, outputDir, logger, ...)
```

## Arguments

- HIP:

  (\`data.frame\`) HIP result

- PPSEpisode:

  (\`data.frame\`)

- PPSMinMax:

  (\`data.frame\`)

- cdm:

  (\`cdm_reference\`)

- outputDir:

  (\`character(1)\`)

- logger:

  (\`logger\`) Logger object

- ...:

  Extra (dev) parameters

## Value

\`NULL\`
