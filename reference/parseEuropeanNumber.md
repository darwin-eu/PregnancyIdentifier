# Parse a character vector of numbers that may use European formatting (dots as thousands separators, commas as decimal separators).

Parse a character vector of numbers that may use European formatting
(dots as thousands separators, commas as decimal separators).

## Usage

``` r
parseEuropeanNumber(x)
```

## Arguments

- x:

  Character vector.

## Value

Numeric vector.
